export type Account = {
  id: number;
  first_name: string;
  last_name: string;
  email: string;
  gender: string;
  ip_address: string;
};

export type Position = {
  x: number;
  y: number;
};

export type Rect = {
  width: number;
  height: number;
};
